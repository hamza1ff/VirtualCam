name: Build and sign VirtualCam APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JAVA_TOOL_OPTIONS: -Xmx3g
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Install SDKMAN and Gradle 8.1.1
        run: |
          curl -s "https://get.sdkman.io" | bash
          source "$HOME/.sdkman/bin/sdkman-init.sh"
          sdk install gradle 8.1.1
          gradle -v

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 33
          build-tools: '33.0.2'

      - name: Decode keystore (safe runtime check)
        run: |
          # إذا لم يُعرَّف SECRET فلن نُحاول فك التشفير
          if [ -z "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "KEYSTORE_BASE64 secret_


cd ~/VirtualCamProject

cat > .github/workflows/android-build.yml <<'YAML'
name: Build and sign VirtualCam APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      JAVA_TOOL_OPTIONS: -Xmx3g
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      - name: Install SDKMAN and Gradle 8.1.1
        run: |
          curl -s "https://get.sdkman.io" | bash
          source "$HOME/.sdkman/bin/sdkman-init.sh"
          sdk install gradle 8.1.1
          gradle -v

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          api-level: 33
          build-tools: '33.0.2'

      - name: Decode keystore (safe runtime check)
        run: |
          # إذا لم يُعرَّف SECRET فلن نُحاول فك التشفير
          if [ -z "${{ secrets.KEYSTORE_BASE64 }}" ]; then
            echo "KEYSTORE_BASE64 secret is empty — skipping keystore decode"
          else
            echo "Decoding keystore from secret..."
            printf '%s' "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > release-keystore.jks
            ls -al release-keystore.jks
          fi

      - name: Build release APK (use installed gradle)
        run: |
          gradle --no-daemon clean assembleRelease

      - name: Align APK
        run: |
          mkdir -p out
          ZIP_PATH="app/build/outputs/apk/release/app-release-unsigned.apk"
          if [ -f "$ZIP_PATH" ]; then
            zipalign -v -p 4 "$ZIP_PATH" aligned-unsigned.apk
          else
            ls -R app/build || true
            echo "Unsigned APK not found at $ZIP_PATH"
            exit 1
          fi

      - name: Sign APK
        run: |
          if [ ! -f release-keystore.jks ]; then
            echo "Keystore not found; cannot sign APK"
            exit 1
          fi
          apksigner sign \
            --ks release-keystore.jks \
            --ks-pass pass:${{ secrets.KEYSTORE_PASSWORD }} \
            --ks-key-alias ${{ secrets.KEY_ALIAS }} \
            --key-pass pass:${{ secrets.KEY_PASSWORD }} \
            --out out/VirtualCam-release-signed.apk aligned-unsigned.apk

      - name: Verify signed APK
        run: apksigner verify --print-certs out/VirtualCam-release-signed.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: VirtualCam-apk
          path: out/VirtualCam-release-signed.apk
