VirtualCam — Android

This project implements a WebView-based virtual camera that streams a user-picked image as a video stream inside the app.
See .github/workflows/android-build.yml for GitHub Actions build & sign workflow.
